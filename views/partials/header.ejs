<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-5 py-3">
        <h3 class="text-xl">Scatch</h3>
        <% var loggedin = typeof loggedin !== 'undefined' ? loggedin : true %>
        <% if(loggedin)  {%>
            <div class="flex gap-5 items-center">
                <a href="/shop">Shop</a>
                <a href="/cart" class="flex items-center gap-1">
                    Cart
                    <span id="cart-counter" class="bg-red-500 text-white rounded-full w-5 h-5 flex items-center justify-center text-xs">
                        <!-- Cart count will be updated dynamically -->
                    </span>
                </a>
                <a href="/account">My Account</a>
                <a class="text-red-600" href="users/logout">Logout</a>
            </div>
        <% }%>
    </nav>